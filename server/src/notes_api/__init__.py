"""Reference server for the Notes API contract.

The contract (``openapi.yaml`` at the repository root) is the source of truth. ``CONTRACT_VERSION`` records
the ``info.version`` this server implements; ``tests/test_smoke.py`` keeps the two in step.
"""

CONTRACT_VERSION = "2.0.0"
