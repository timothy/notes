# Alembic configuration for the server schema. The database URL comes from DATABASE_URL when it is
# not set here (see alembic/env.py); the test harness sets sqlalchemy.url programmatically.
[alembic]
script_location = %(here)s/alembic
file_template = %%(rev)s_%%(slug)s
